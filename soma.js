// Crie uma função chamada `somar` que receba dois números como parâmetro e exiba a soma no console.

function soma(a,b) {
    console.log(`A soma de dois número `)
}

// soma(5,2)

// Crie uma função chamada dobro(numero) que receba um número como parâmetro e exiba o dobro desse número no console.

function dobro(numero){
    console.log(numero*2)
}

// dobro(5)


// Crie uma função chamada multiplicar que receba dois número como parâmetro e exiba o resultado dessa multiplicação no console.

function multiplicar(a,b){
    console.log(a*b)
}

// multiplicar(2,4)

// cont saudacao = function(parâmetro)=>

// função com retorno
function nomeDaFuncao(n1){
    return n1 * 2
}
let result = nomeDaFuncao(3)
// console.log(result)

// crie uma função chama triplo(numero) que receba um número e retorne o triplo dele

function triplo(numero){
    return numero*3
}
let resultad = triplo(10)
// console.log(resultad)

// Crie uma função chamada celsiusParaFahrenheit(celsius) que converta e retorne a temperatura em Fahrenheit.

function celsiusParaFahrenheit(celsius){
    return (celsius*1.8)+32
}
let retorne = celsiusParaFahrenheit(20)
// console.log(retorne)

// arrow function
const dobrar = (n1) => {
    console.log(n1 * 2)
}

// dobrar (10)

// arrow function simplificado
const dobra = (n1) => n1 * 2

console.log(dobra (3))

